2026-02-20

Major changes
- Match flow is now centralized in `main/match_director.gd` with explicit states:
  `LOBBY`, `RUNNING`, `WON`, `LOST`, `RESETTING`.
- MatchDirector now acts as the single server-side source of truth for:
  score, lives, deaths, and shared objective progress.
- Added/solidified event-style API on MatchDirector:
  `register_player_spawn`, `report_player_fell`, `report_enemy_killed`,
  `report_objective_progress`, `request_respawn`, `set_player_lives`.
- MatchDirector respawn permission is now enforced centrally via `request_respawn`.
- Match snapshot broadcasting remains centralized through one RPC
  (`_receive_snapshot`) consumed by UI.

Gameplay script refactor
- `main/fall_checker.gd` now reports falls to MatchDirector instead of owning
  its own independent life counters.
- `enemies/bee_bot.gd` now reports kills through
  `report_enemy_killed("bee_bot", killer_id)` instead of direct score mutation.
- `player/player.gd` now synchronizes enemy-hit life changes back into
  MatchDirector to keep counters coherent.
- `ui/ui.gd` now reads connection info + MatchDirector snapshot only
  (removed separate FallChecker lives feed).

Tests
- Extended `test/test_match_director.gd` with life-loss and enemy-kill objective tests.
- Updated `test/test_fall_lives.gd` to validate FallChecker + MatchDirector integration.
- Full GUT suite currently passes in headless run (20/20).

Bugfix update
- Fixed a life-counter rebound where bomb damage could reduce player lives by 1 and
  then immediately restore it due to stale server-side lives state.
- Root cause: damage was applied on node authority peer, while MatchDirector lives
  remained unchanged on server; FallChecker later re-synced stale lives back.
- `player/player.gd` enemy damage flow is now server-authoritative and updates both:
  1) MatchDirector (`set_player_lives`) and
  2) owning player peer (`set_lives` sync).
- Extended integration coverage in
  `test/integration/test_bomb_player_integration.gd` to assert:
  - bomb hit decrements player lives,
  - director lives are updated identically,
  - FallChecker sync does not cause lives to bounce back.

Gameplay update
- Added slope sliding behavior in `player/player.gd`:
  - `slide_start_angle_deg`: slope angle where sliding starts.
  - `slide_block_angle_deg`: slope angle where uphill input is fully blocked.
  - `slope_slide_accel`: downhill tangential acceleration on steep slopes.
  - `slope_slide_friction`: damping to keep slide speed stable.
- Implementation details:
  - Uphill input is progressively reduced between start/block angles.
  - A downhill slide impulse is applied when standing on steep ground.
  - Flat terrain and gentle slopes keep normal controls.

Sliding visual feedback update
- Added explicit sliding visual state for players:
  - synchronized flag `_is_sliding_sync` replicated via `player/player.tscn`.
  - authority-driven `set_sliding(...)` in `player/model/character_skin.gd`.
- While sliding:
  - locomotion animation is paused from walk/run state updates,
  - fallback pose uses existing `fall` animation state for readability,
  - `CharacterRotationRoot` gets a configurable forward tilt for clearer feedback.
- New player tuning parameters:
  - `slide_visual_tilt_deg`
  - `slide_visual_lerp_speed`

Sliding behavior tuning update
- Removed static `fall` pose fallback during slope sliding; sliding now keeps
  locomotion animation active for better readability.
- Downhill motion is now speed-target based:
  - accelerates toward `slope_downhill_speed` only along downhill direction,
  - does not boost uphill movement,
  - damps lateral drift only (`slope_lateral_damping`).
- Sliding visual state is now tied to actual downhill speed
  (`slide_visual_speed_threshold`), so stopping on a slope returns to normal
  standing/idle presentation.

Slope contact stability update
- Improved player-to-ground contact on steep slopes in `player/player.gd`:
  - added `slope_floor_snap_length` (default `0.55`),
  - applies `apply_floor_snap()` while descending and not jumping.
- This reduces the "hovering above slope" effect and limits contact loss
  during steep downhill movement.

Downed / revive loop update
- Added a server-authoritative downed revive mechanic using `Coin`.
- `Player` changes:
  - dead players now remain visible ("ghost view") instead of being hidden,
  - dead players are added to `downed_players` group,
  - added `can_be_revived()` and `try_revive_with_coin()` methods,
  - revive restores exactly `1` life (not full lives) and re-enables control.
- `Coin` changes:
  - server scans nearby downed players and can auto-target them,
  - if any downed player exists, normal alive pickup is blocked to preserve revive usage,
  - on collect, coin first attempts revive; otherwise falls back to normal pickup.
- `FallChecker` changes:
  - when a fall causes life to reach `0`, player is respawned back on the map as downed
	so teammates can revive (instead of staying unreachable under the map).
- Added integration coverage:
  - `test/integration/test_coin_revive.gd`
  - updated `test/test_fall_lives.gd` for downed-respawn-at-zero behavior.

Downed/revive polish update
- Coin consumption is now synchronized across peers:
  - coin becomes unavailable/hidden immediately when consumed,
  - coin is not reusable after a revive.
- Coin consume sound (`player/coin/audio/completetask_0.mp3`) is now triggered
  at actual revive/consume time for all peers.
- Added clearer downed visual feedback on player nickname labels:
  - suffix `"[DOWNED]"`,
  - red-tinted nickname color while downed.

Bomb utility gameplay update
- Bombs now emit a utility event to world objects in group `bomb_reactives`.
  - Implemented in `main/bomb.gd` via `_notify_bomb_reactives()`.
  - Reactives receive `on_bomb_exploded(world_pos, radius, owner_peer_id)`.
- Added a reusable example utility object:
  - `main/bomb_door.gd` (`BombDoor`) opens when hit by enough nearby bomb blasts.
  - Supports objective reporting to `MatchDirector` via `objective_id`.
- Added a demo instance in `main/main.tscn`:
  - `BombDoorDemo` (orange barrier) configured with one bomb required.
- Added integration coverage:
  - `test/integration/test_bomb.gd::test_bomb_notifies_reactive_world_objects`.

Scene hierarchy cleanup update
- Reorganized `HubLevel/Env` content in `main/main.tscn` with dedicated folders:
  - `Enemies` (contains `bee_bot`),
  - `Interactives` (contains `Coin`, `BombDoor`),
  - `PhysicsObjects` (contains `RigidCube3D*`).
- Goal: reduce root clutter in `Env` and make gameplay object ownership clearer.

Bomb door visual effect update
- `BombDoor` now supports two open behaviors:
  - `LIFT` (previous behavior),
  - `DISINTEGRATE` (new default behavior).
- `DISINTEGRATE` effect combines:
  - smoke puff spawn,
  - mesh scale-down tween,
  - transparency fade-out,
  - collision disabled immediately.
- Added integration coverage:
  - `test/integration/test_bomb_door.gd`.

Match timer UI update
- Added a top-center match timer label for all players in `ui/ui.tscn` (`InGameUI/MatchTimer`).
- `ui/ui.gd` now parses MatchDirector snapshots and displays:
  - match state (`LOBBY/RUNNING/WON/LOST/...`),
  - remaining time as `MM:SS`.
- Added null-safe handling so existing minimal UI tests/scenes without this label
  still run without errors.

Bee friendly-fire fix
- Prevented bee projectiles from killing other bees.
- `enemies/bee_bot.gd`: bees are now tagged in group `bee_bots`.
- `player/bullet.gd`: bullet collision handler ignores damage when both shooter
  and collider are in `bee_bots`, and projectile now passes through allied bees.

Bomb throw feel and visibility update
- Improved bomb input and throw behavior in `player/player.gd`:
  - right mouse button now throws bombs (key `B` kept as fallback),
  - spawn point is now computed from camera forward direction with upward offset,
  - initial throw velocity is applied for a proper arc/roll behavior.
- Bomb scene switched to dynamic physics:
  - `main/static_body_3d_bomb.tscn` root changed to `RigidBody3D`,
  - added `MultiplayerSynchronizer` replication for transform and velocities.
- `main/bomb.gd` now extends `RigidBody3D` to match the scene and support
  physically dynamic movement after spawn.

Cooperative pull-cube puzzle update
- Added a new pull interaction on green rigid cubes (`main/rigid_body_3d.gd`):
  - left click from a player now toggles attach/detach on a pullable cube
	(implemented in `player/player.gd` via camera raycast),
  - each attached player contributes a traction vector from cube to player,
  - vectors naturally cancel when players pull in opposite directions,
  - vectors accumulate when players pull from the same side.
- Added visual state feedback synchronized to all peers:
  - `IDLE` (green),
  - `ATTACHED` (yellow),
  - `COOP` (orange),
  - `GOAL` (blue, cube frozen in reactor zone).
- Added reactor objective check:
  - cubes auto-detect the scene node named `reactor`,
  - when inside `reactor_goal_radius`, cube marks goal reached and freezes.
- Replication update in `main/main.tscn`:
  - `SceneReplicationConfig_fwcvl` now also replicates `._pull_state_sync`.
- Added test coverage in `test/test_pull_cube.gd`:
  - pull vectors accumulate on same-side pulls,
  - pull vectors cancel on opposite pulls,
  - reactor goal state transitions to `GOAL` correctly.
- Added a visible `RayCast3D` (`PlayerCamera/PullRay`) that mirrors `_try_toggle_pull_cube()`
  so the left-click ray appears in Debug mode; the ray is updated every call based
  on `pull_interaction_distance`.
- When two players stay aligned and the cube is in `COOP` (orange) state, it now
  auto-drives toward the `reactor` once their pull direction aligns; while auto-moving
  the emission energy grows (`auto_move_glow_boost`) so the team can see the assist
  engage before the cube reaches `GOAL`.
- Door/bomb synchronization fix:
  - `main/bomb_door.gd` now marks `_set_open_state` as an `all_peers` RPC so the
	server-driven opening state is propagated to every client instead of staying
	local to the authority.
 

Player architecture refactor (domain components)
- Refactored `player/player.gd` into an orchestrator (214 lines) and extracted
  gameplay domains into dedicated components under `player/components/`:
  - `player_movement.gd` (movement, slope rules, animation driving, push physics),
  - `player_combat.gd` (melee attack + server-authoritative enemy hit handling),
  - `player_lifecycle.gd` (lives, dead/downed state, revive flow),
  - `player_net_sync.gd` (replicated sync properties + client interpolation),
  - `player_interactions.gd` (bomb spawn/throw, pull-cube interaction, coin collect).
- Preserved public `Player` API and RPC entrypoints used by existing systems/tests:
  - `spawn_bomb`, `respawn`, `set_dead_state`, `set_lives`, `_collect_coin`,
	`_request_enemy_hit_on_server`, `damage`, `try_revive_with_coin`, etc.
- Goal: improve readability, reduce single-file cognitive load, and make future
  changes safer by isolating movement/combat/network/lifecycle concerns.
- Validation: full GUT test suite passes after refactor (`22/22`).

2026-04-05
- Scarabées: la scène principale ne mélange plus un scarabée seed hors directeur avec des scarabées dynamiques ; le `BeetleDirector` est désormais l'unique source des scarabées actifs.
- Scarabées: ajout d'une troisième ancre autour de l'Activator pour éviter les spawns improvisés en anneau et rendre la défense de zone plus stable à 2-4 joueurs.
- Scarabées: le `BeetleDirector` s'appuie maintenant sur une zone de défense explicite (`Activator`) et sur des graines explicites, au lieu d'un scan global implicite de la scène.
- Scarabées: les ancres dynamiques ont été replacées autour de l'Activator pour éviter les apparitions lointaines incohérentes et recentrer la défense sur la zone à protéger.
- Tests: ajout de régressions GUT vérifiant la résolution de la zone de défense et des graines explicites du `BeetleDirector`.
- Scarabée: ralentissement global de la poursuite et de la charge, avec un comportement recentré sur la défense de la zone Activator plutôt qu'une poursuite sur toute la map.
- Scarabée: ajout d'un leash de garde autour de la position d'origine, avec retour automatique vers la zone si la cible ou le scarabée s'éloigne trop.
- Directeur scarabées: progression de vitesse par joueur adoucie pour garder une difficulté défensive plutôt qu'hyper agressive.
- Tests: ajout d'une régression GUT vérifiant qu'un scarabée abandonne une cible trop lointaine et revient vers sa zone de garde.
- Joueur: freinage horizontal renforcé sans input côté autorité et interpolation distante pour supprimer le glissement résiduel après relâchement des touches.
- Joueur: garde supplémentaire dans `player/components/player_movement.gd` pour éviter les calculs de pente avec une normale de sol nulle.
- Scarabée: synchro de l'état visuel `Idle/Walk/Attack/PowerOff` depuis l'autorité pour éviter les pattes figées côté clients pendant la poussée.
- Scarabée: poursuite améliorée via waypoint sur `BombDoor` ouverts lorsque la cible est de l'autre côté du mur, plus un léger `unstuck` serveur quand la charge est bloquée par la géométrie.
- Tests UI: ajout du scénario `test/UI/test_beetle_door_charge_ui.sh` qui ouvre réellement le mur puis vérifie qu'un scarabée ciblant `client_2` se rapproche effectivement de lui.
- Tests GUT: ajout de `test/test_player_net_sync.gd` pour verrouiller le freinage réseau d'un joueur sans direction répliquée.
- Tests UI scarabées: stabilisation du scénario `beetle_targeting` pour attendre le vrai quorum 4 participants / 3 joueurs actifs avant de snapshotter la répartition des cibles.
- Scarabée: ajout d'un retour rapide au sol côté autorité dans `enemies/beetle_bot.gd`.
- Scarabée: le raycast de sol ignore désormais les corps dynamiques et les autres entités vivantes pour éviter qu'un scarabée "surfe" sur un cube ou un joueur.
- Tests: ajout d'un test GUT vérifiant qu'un scarabée soulevé redescend bien vers le sol dans `test/test_beetle_bot.gd`.
- Audit late join: `PullableCube` pousse maintenant explicitement son état courant aux nouveaux peers et accepte une demande de resynchronisation client dans `main/rigid_body_3d.gd`.
- Audit late join: `InventoryContainer3D` complète maintenant sa stratégie snapshot par un push serveur sur `peer_connected` en plus de la demande client dans `inventory/inventory_container.gd`.
- Tests: ajout d'un test GUT de resynchronisation late join du cube dans `test/test_pull_cube.gd`.
- Mission cube: suppression du double comptage de l'objectif entre `CubeActivator` et `PullableCube` dans `main/cube_activator.gd`.
- UI debug: ajout d'un overlay minimal activable avec `F3` pour afficher état du match, réseau, résumé inventaire, cibles ennemies et mesures de sync.
- Scarabée: reprise robuste de cible via balayage de la zone de détection et fallback par distance sur le groupe `players`, pour éviter un blocage après ouverture du mur.
- Scarabée: locomotion rapprochée plus fluide avec distance d'arrêt réduite et animation de marche conservée au contact.
- Tests: ajout de régressions scarabée sur acquisition de cible sans signal et maintien de l'état `Walk` en poussée proche.
- Scène principale: suppression d'une instance parasite de `GroundEnemy` à la racine de `main/main.tscn`, qui créait un scarabée "fantôme" physiquement actif hors du flux normal du directeur.
- Scarabée: `set_director_active(false)` désactive maintenant aussi collisions et détection pour qu'un scarabée inactif ne puisse plus pousser le joueur invisiblement.
- Tests: ajout d'une régression GUT vérifiant qu'aucun scarabée n'est instancié à la racine de `main/main.tscn`.
- Tests UI scarabées: le scénario `beetle_door_charge` attend désormais aussi le nombre attendu de scarabées avant de conclure, pour éviter un faux négatif pendant la montée en charge des clients.
- Tests UI scarabées: le scénario `beetle_targeting` attend maintenant aussi la redistribution effective des cibles avant de snapshotter, pour éviter un état intermédiaire avec des cibles encore dupliquées.
- Bombes: le spawn de bombe utilise désormais explicitement la scène courante comme parent dans `player/components/player_interactions.gd`, ce qui évite un échec RPC dans les scénarios UI scarabées.

2026-04-09
- Refactor structurel: extraction de `main/mission_hub_enemies.tscn` pour sortir le runtime `Enemies` de `main/main.tscn` tout en gardant les mêmes chemins logiques.
- Refactor structurel: extraction de `main/mission_cube_physics_objects.tscn` pour sortir `PhysicsObjects` de `main/main.tscn` sans casser `HubLevel/Env/PhysicsObjects/...`.
- Ennemis: ajout de `enemies/enemy_instance_base.gd` pour homogénéiser la révision d'état, le contrat de resync/debug et les groupes runtime des instances ennemies.
- Ennemis: `BeeBot` et `BeetleBot` héritent maintenant d'une base commune légère, comme `BeeDirector` / `BeetleDirector` héritaient déjà de `EnemyDirectorBase`.
- Objets persistants: ajout du groupe générique `replicated_persistent_objects` sur les objets gameplay durables majeurs pour rendre le debug et les tests moins fragiles.
- Debug réseau/gameplay: `Connection` conserve maintenant aussi un historique structuré des événements de sync (`get_recent_sync_event_entries`, `clear_recent_sync_events`).
- UI debug: le mode `F3` agrège maintenant les résumés via le groupe `replicated_persistent_objects` au lieu de reposer sur une liste d'objets codée en dur.
- Tests: ajout de `test/test_connection_debug_events.gd`, `test/test_main_scene_structure.gd` et `test/integration/test_persistent_late_join_sync.gd`.
- Tests: `test/test_replicated_persistent_contract.gd` verrouille désormais aussi le groupe `replicated_persistent_objects`.
- Scène principale: formalisation de groupes gameplay stables dans `main/main.tscn` (`mission_cube_goal_zones`, `mission_cube_bomb_doors`, `mission_cube_blockers`, `mission_cube_primary`, `mission_hub_chests`, `enemy_directors`, etc.) pour découpler les scénarios/tests des noms de nœuds fragiles.
- Tests UI: `player/components/player_ui_test_driver.gd` utilise désormais ces groupes stables pour retrouver coffre, portes, Activator, cube principal et directeur scarabées.
- Ennemis: unification légère du contrat `director/enemy` pour abeilles et scarabées avec groupes runtime communs (`enemy_directors`, `enemy_instances`) et méthodes homogènes (`apply_director_config`, `set_director_active`, getters de cible).
- Abeilles: `BeeDirector` supporte maintenant des graines explicites via `managed_seed_bee_paths`, alignées sur le style déjà utilisé côté scarabées.
- Coffre: `InventoryContainer3D` diffuse maintenant des deltas de slots modifiés quand ils sont plus compacts qu'un snapshot complet, tout en conservant les snapshots complets pour le late join et la récupération.
- Tests: ajout de `test/test_enemy_director_conventions.gd` et `test/test_inventory_container.gd` pour verrouiller respectivement le contrat directeurs/ennemis et la logique de delta du coffre.
- Modularisation: extraction de `main/mission_cube_beetle_director.tscn` et `main/mission_cube_interactives.tscn` pour alléger `main/main.tscn` sans changer les chemins racine utiles (`BeetleDirector`, `Interactives`).
- Debug réseau/gameplay: `Connection` conserve maintenant un historique borné des événements de synchronisation (`record_sync_event`) consulté par le panneau `F3`.
- UI debug: le mode `F3` affiche désormais plus de détail sur le `MatchDirector` (vies, scores, morts), le mode de réplication du coffre, l'état du coin de résurrection et les événements récents de sync.
- Coin: le coin de résurrection suit maintenant la checklist late join au lieu d'être `queue_free` à la consommation ; il reste caché/désactivé et pousse explicitement son état courant aux nouveaux peers.
- Coin: ajout d'une synchronisation de cible/consommation pour éviter qu'un coin utilisé ou déplacé réapparaisse chez un client après une revive.
- Observabilité: `BombDoor`, `WorldItem`, `InventoryContainer3D`, `MatchDirector` et `Coin` enregistrent désormais des événements de sync utiles au debug multi.
- Tests: `test/integration/test_coin_revive.gd` couvre maintenant la persistance du coin consommé, la réapplication d'état late join et la mémorisation de la cible de revive.
- Tests UI: `test/test_ui_escape.gd` vérifie aussi que l'overlay `F3` expose bien les sections debug et les événements récents de synchronisation.
- Architecture: formalisation dans `ARCHITECTURE_PROJET.md` d'une convention publique commune pour les objets persistants répliqués (`request_current_state_from_server`, `push_current_state_to_peer`, `get_state_revision`, `get_debug_sync_summary`).
- Objets persistants: alignement de `BombDoor`, `WorldItem`, `InventoryContainer3D`, `Coin`, `Box`, `PullableCube` et `MatchDirector` sur cette convention, avec révisions d'état et résumés debug homogènes.
- Ennemis: `BeeBot` et `BeetleBot` exposent désormais la même API publique de resync/debug que les autres objets persistants.
- Directeurs ennemis: extraction d'une base légère commune `enemies/enemy_director_base.gd` pour mutualiser groupes, révision d'état, debug et helpers de scène.
- Directeurs ennemis: `BeeDirector` et `BeetleDirector` héritent maintenant de cette base et exposent eux aussi la convention publique de resynchronisation/debug.
- UI debug: l'overlay `F3` consomme maintenant prioritairement `get_debug_sync_summary()` quand un objet persistant l'expose.
- Modularisation: extraction de `main/mission_cube_goal_zone.tscn` pour sortir `Activator/CubeActivator` de `main/main.tscn` tout en gardant le même chemin de scène logique.
- Tests: ajout de `test/test_replicated_persistent_contract.gd` pour verrouiller le contrat commun et la progression des révisions sur les objets persistants majeurs.
- Tests: `test/test_enemy_director_conventions.gd` couvre maintenant aussi l'API publique de resync/debug des directeurs et des ennemis.

2026-04-10
- Level design: extraction de `main/mission_zone_verger_enemies.tscn` pour découpler les abeilles du verger du runtime ennemis du hub.
- Level design: ajout de `main/mission_zone_breche_enemies.tscn` avec un `BeetleDirector` dédié à la brèche, activé par portail + présence joueur autour d'un `DefenseMarker`.
- Tests/UI: `portal_progression` écrit maintenant aussi ses phases `breche_unlocked` et `reactor_unlocked` côté serveur, ce qui supprime une fragilité de synchro entre observation client et progression autoritaire.
- Tests: `test/test_beetle_director.gd` et `test/test_main_scene_structure.gd` couvrent désormais explicitement le directeur scarabées de la brèche.
- Tests UI scarabées: ajout de `main/ui_test_scenario_server_pilot.gd`, un pilote serveur dédié au scénario `beetle_targeting`, pour préparer le monde de façon autoritaire avant observation côté clients.
- Tests UI scarabées: `beetle_targeting` ne dépend plus d'un simple repositionnement client dans `PlayerUiTestDriver`; le serveur place maintenant les joueurs autour du réacteur, pousse l'état du `BeetleDirector` et attend les accusés de réception clients.
- Tests: ajout de `test/test_ui_test_scenario_server_pilot.gd` pour verrouiller la présence du pilote dans `main/main.tscn` et la stabilité des slots joueurs autour de l'Activator.
- Level design: la scène principale compose maintenant explicitement `HubLevel`, `ZoneScierie`, `ZoneVerger`, `ZoneBreche` et `ZoneReactor`, avec progression multi-zone via portails.
- Hub: le portail vers le réacteur a été replacé sur la terre ferme pour rester atteignable dans le hub.
- Mission cube: le driver UI attend désormais un vrai état de caisse détruite via `Box.is_destroyed()` avant de poursuivre le scénario.
- Mission cube: la trajectoire coop de poussée du cube a été recalée sur la nouvelle géométrie du réacteur, ce qui remet `test/UI/test_cube_mission_ui.sh` au vert.
- Tests UI: le scénario `beetle_targeting` est maintenant centré sur les scarabées de la zone réacteur et ne compte plus ceux de la scierie.
- Scarabées: suppression d'un push d'état redondant sur `peer_connected` côté instance, qui pouvait envoyer des RPC à un scarabée dynamique pas encore instancié côté client.
- Tests UI: le wrapper `portal_unlock` tolère désormais les fichiers JSON en cours d'écriture pour éviter un faux négatif sur lecture partielle.
- Validation: base retrouvée verte avec `66/66` tests GUT passés et les wrappers UI `inventory_chest`, `portal_unlock`, `cube_mission` et `beetle_targeting` passants.
- Tests UI: ajout du scénario `test/UI/test_portal_logistics_ui.sh`, qui valide un vrai aller-retour multi-zone via portails avec dépôt de bois et de pommes dans le coffre du hub.
- Tests UI: le driver `portal_logistics` déclenche maintenant explicitement l'entrée dans les portails et choisit les pickups les plus proches par groupe stable au lieu de dépendre de noms globaux.
- Tests UI: le scénario `portal_unlock` a été réaligné sur les groupes `mission_wood_pickups` et `mission_apple_pickups`, ce qui le rend compatible avec la nouvelle map multi-zone.
- Validation: la base est maintenant verte avec `67/67` tests GUT passés, plus les wrappers UI `inventory_chest`, `portal_unlock`, `portal_logistics`, `cube_mission` et `beetle_targeting`.
2026-04-11
- Tests UI: ajout de `test/UI/run_ui_suite.sh` avec profils `smoke`/`full` et sélection par fichiers modifiés (`--changed`) pour réduire le coût des validations locales.
- Tests UI: `test_portal_progression_ui.py` est maintenant découpé en modes `--phase breche|reactor|full`.
- Tests UI: ajout des wrappers `test_portal_progression_breche_ui.sh` et `test_portal_progression_reactor_ui.sh`.
- Tests GUT E2E: `test/test_ui_e2e.gd` supporte désormais `UI_E2E_PROFILE=smoke|full` (smoke ciblé par défaut de profil côté logique du test).
- Intégration: `test/integration/test_portal_progression.gd` découpe les préconditions serveur en tests séparés (phase brèche et phase réacteur).
- Documentation: ajout d'une matrice initiale "fichiers modifiés -> tests UI recommandés" dans `ARCHITECTURE_PROJET.md`.
- BombDoor: durcissement de `main/bomb_door.gd` pour ne plus planter si la hiérarchie visuelle est modifiée en éditeur (résolution dynamique de `MeshInstance3D` / `CollisionShape3D`, gardes nulles avant accès à `.scale`/`.visible`).
- Main scene: conversion explicite des sous-nœuds de `ZoneBreche/Interactives/BombDoor4` et `BombDoor7` en nœuds typés dans `main/main.tscn` pour supprimer les overrides instables "modified from inside an instance... vanished".
- Tests: assouplissement de `test/test_beetle_director.gd` pour accepter `>= 3` portes de mission, afin de rester compatible avec l'ajout de portes dans la brèche.
- Validation: `96/96` GUT passés et `test/UI/test_portal_progression_ui.sh` vert après correction.
- Portails: ajout d'un libellé de destination dans `levels/portal/portal.tscn` et `levels/portal/portal.gd`, avec état visuel explicite `BLOQUE` rouge / `OUVERT` vert.
- Portails: ajout de pylônes guides colorés autour de chaque portail pour renforcer la lisibilité à distance sans changer la logique de progression.
- Zones: ajout de marqueurs visuels dédiés dans `zone_scierie.tscn`, `zone_verger.tscn`, `zone_breche.tscn` et `zone_reactor.tscn` pour donner une identité immédiate à chaque biome.
- Tests: `test/integration/test_portal_progression.gd` vérifie maintenant aussi le texte d'état du portail (`BLOQUE` / `OUVERT`) en plus de la couleur rouge/verte.
- Validation: `96/96` tests GUT passés, plus `test_portal_unlock_ui.sh`, `test_portal_progression_ui.sh` et `test_portal_logistics_ui.sh` verts.
- Tests UI: `portal_unlock` passe maintenant par une préparation autoritaire côté serveur via `main/ui_test_scenario_server_pilot.gd`, sur le même modèle que `beetle_targeting` et `beetle_door_charge`.
- Tests UI: le pilote serveur place explicitement `client_a` près du bois et `client_b` près des pommes, attend les accusés de réception clients, puis laisse le scénario observer les dépôts réels au coffre.
- Tests UI: `player/components/player_ui_test_driver.gd` ne téléporte plus directement les clients pour `portal_unlock`; il se contente désormais de piloter les états gameplay après préparation serveur.
- Validation: `test/UI/test_portal_unlock_ui.sh` repasse vert avec `portal_breche_active = true` sur serveur et clients.
- Validation: batch UI complet repassé vert (`inventory_chest`, `inventory_player_proximity`, `inventory_transfer_multiplayer`, `late_join_bomb_wood`, `portal_unlock`, `portal_logistics`, `portal_progression`, `cube_mission`, `cube_mission_lock`, `beetle_targeting`, `beetle_door_charge`).
- Tests UI: stabilisation du scénario `late_join_bomb_wood` en ciblant un pickup bois déterministe (même objet côté client A/B) et en forçant un spawn de bombe au contact de la porte.
- Tests UI: `test_portal_unlock_ui.sh` est maintenant un wrapper de compatibilité qui délègue à `test_portal_progression_ui.py --phase breche` (même objectif métier, scénario moins fragile).
- Tests UI: ajout d'un runner `smoke/full` opérationnel (`test/UI/run_ui_suite.sh`) validé avec `--profile smoke`, `--profile full` et `--changed`.
- Validation: `99/99` GUT passés, `test/UI/run_ui_suite.sh --profile smoke` vert, `test/UI/run_ui_suite.sh --profile full` vert.
- Level design: ajout de signalétique de navigation dans `levels/hub_level.tscn` via `MissionMarkers/HubSign_*`, avec 4 repères colorés orientant vers `SCIERIE`, `VERGER`, `BRECHE` et `REACTOR`.
- Documentation: `PlanLevelDesign.txt` et `ARCHITECTURE_PROJET.md` mis à jour pour refléter cette étape terminée et la prochaine suite visuelle (variations de décor par biome).
- Validation: `99/99` GUT verts et `test/UI/run_ui_suite.sh --profile smoke` vert après ajout de signalétique.
- Validation/Blocage: `test/UI/test_beetle_door_charge_ui.sh` en échec sur 3 essais (flaky RPC nœuds dynamiques: `@RigidBody3D@...` / `Main/StaticBody3DBomb/MultiplayerSynchronizer`), ce qui maintient `UI full` non stable.
2026-04-12
- Outils: migration des scripts UI Python vers le binaire Godot `4.6.2` avec fallback configurable via `GODOT_BIN` (valeur par défaut `/dataSSD/Godot_v4.6.2-stable_linux.x86_64`).
- Rendu: stabilisation de la luminosité inter-version dans `defaults/world_environment.tres` (tonemap/exposition/brightness explicites) et lumière directionnelle explicite dans `levels/hub_level.tscn`.
- Documentation: ajout du chemin binaire de référence et de la surcharge `GODOT_BIN` dans `ARCHITECTURE_PROJET.md` et `test/UI/README.md`.
- UI stress: robustesse augmentée dans `test/UI/test_replication_stress_ui.py` (timeout adaptatif, désactivation explicite des scarabées de fond) et migration du scénario `replication_stress` vers les groupes de pickups stables dans `player/components/player_ui_test_driver.gd`.
- Validation 4.6.2: GUT complet `99/99` vert, `UI smoke` vert, `UI full` exécute 10/11 scénarios puis reste exposé au flaky connu `test_beetle_door_charge_ui.sh` (RPC nœuds dynamiques) ; exécution isolée de `test_beetle_door_charge_ui.sh` verte.
- Validation 4.6.2: `test/UI/test_replication_stress_ui.sh` reste en échec (fichiers `replication_stress_client_*` non produits), cas préexistant/structurel de ce scénario de charge à investiguer séparément.
- Rendu 4.6.2 (ajustement 2): réduction nette des ombres bouchées via `DirectionalLight3D` (opacité d’ombre abaissée, blur augmenté) + ajout d’une `DirectionalLightFill` sans ombre dans `levels/hub_level.tscn`.
- Rendu 4.6.2 (ajustement 2): hausse de l’éclairage global et de l’exposition dans `defaults/world_environment.tres` (`ambient_light_energy`, `tonemap_exposure`, `adjustment_brightness`) pour éviter les silhouettes noires.
- Validation post-correctif rendu: démarrage headless OK, GUT `99/99` vert et `UI smoke` vert sous `/dataSSD/Godot_v4.6.2-stable_linux.x86_64`.

2026-04-15
- Interaction inventaire: un tonneau pousseable devant le joueur bloque désormais correctement l'interaction du coffre derrière (pas d'ouverture/focus à travers obstacle).
- Tests: ajout de `test/test_player_interactions.gd` avec régression unitaire sur le cas "coffre bloqué par tonneau" + contrôle du cas nominal coffre accessible.
- QA/itération locale: usage de `DEV_SPAWN_ZONE` conservé pour accélérer les cycles de validation UI longs, avec gain constaté limité selon la géométrie/état des zones.
